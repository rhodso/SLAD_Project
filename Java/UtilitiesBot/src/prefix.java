public class prefix {
    private static String prefix = "!";

    /**
     * @return the prefix
     */
    public static String getPrefix() {
        return prefix;
    }

    /**
     * @param _prefix the prefix to set
     */
    public static void setPrefix(String _prefix) {
        prefix = _prefix;
    }
}